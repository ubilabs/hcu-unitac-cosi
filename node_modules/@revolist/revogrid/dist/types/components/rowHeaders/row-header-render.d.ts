import { VNode } from '../../stencil-public-runtime';
import { RevoGrid } from '../../interfaces';
export declare const RowHeaderRender: (start: number) => (_h: RevoGrid.HyperFunc<VNode>, e: {
  rowIndex: number;
}) => number;
